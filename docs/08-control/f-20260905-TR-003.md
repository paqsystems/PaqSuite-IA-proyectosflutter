# Paso F — Verificar implementación vs documentos — TR-003

Fecha: 2026-09-05  
Equivalente: openspec-05

## 1. Contexto

| Artefacto | Ruta |
|-----------|------|
| SPEC | `docs/02-producto/SPEC-AGW-001-producto.md` §6.1 |
| HU | `docs/03-historias-usuario/001-Conectividad/HU-002-gateway-aws.md` (CA 9–13) |
| TR | `docs/04-tareas/001-Conectividad/TR-003-deploy-gateway-aws.md` (N1–N6) |
| C1 / D1 / F1 | `c1-20260905-TR-003.md`, `d1-20260905-TR-003.md`, `f1-20260905-TR-003.md` |
| “Código” / artefacto | Runbook + plantillas `docs/06-operacion/deploy/*` + instancia AWS *Paq-Gateway-IA* (fuera del repo) |

TR-003 no modifica `src/PaqGateway`; el contraste es **SPEC/HU/TR ↔ docs de ops ↔ despliegue real**.

## 2. Resumen

TR-003 cumple el alcance de publicación AWS (EC2 misma VPC, systemd, Nginx TLS, DNS operativo, `/internal` privado, secretos por env, instructivo actualizado). CA 9, 11–13 con evidencia. CA 10 (handshake WSS con agente válido) **no** cerrado. Hostname canónico ops `gateway.paqsystems.com` diverge del literal SPEC/HU `gateway.paqsuite.com` (documentado; dominio `paqsuite.com` ausente en Route 53).

Sin hallazgos **CRÍTICOS** de cierre falso del deploy base. El humano puede marcar **Finalizado TR-003** si acepta CA 10 como pendiente explícita (o tras correr WSS).

## 3. Completitud / Corrección / Coherencia

### Completitud (CA 9–13 + N1–N6 + tareas TR)

| Ítem | Veredicto |
|------|-----------|
| CA9 EC2 misma VPC, .NET 8, systemd, reverse proxy TLS | ✓ *Paq-Gateway-IA* / Nginx+LE / unit active |
| CA10 `…/agent-hub` acepta handshake WSS | ⚠ TLS+proxy OK (`400` GET); **WSS agente no corrido** |
| CA11 SG 443 público; 1433 no a Internet; Laravel→`/internal` privado | ✓ SG documentado + curls; internal público 404 |
| CA12 Secretos por entorno; instructivo actualizado | ✓ `/etc/paqgateway/env` + runbook + instalacion-exhaustiva |
| CA13 Laravel GET status por red privada + API key | ✓ Forge → `10.0.1.224:5100` 200 |
| N1 EC2 | ✓ |
| N2 Nginx en EC2 | ✓ |
| N3 DNS → IP pública | ✓ `gateway.paqsystems.com` (no `paqsuite.com`) |
| N4 Kestrel + `/internal` no público | ✓ `0.0.0.0:5100` + SG (evolución vs C1 `127.0.0.1`; justificada) |
| N5 Sin stub Dev en prod | ✓ |
| N6 Publish `/opt` + systemd | ✓ |
| Plantillas service/nginx/env + publish documentado | ✓ |

### Corrección (SPEC §6.1)

| Tema | Veredicto |
|------|-----------|
| Misma VPC que Laravel; una instancia MVP | ✓ |
| HTTPS/WSS 443; hub `/agent-hub` | ⚠ HTTPS sí; WSS e2e agente no demostrado |
| Kestrel interno; Nginx termina TLS + Upgrade | ✓ (plantilla + conf real) |
| SG: 443 agentes; 1433 no Internet; `/internal` privado | ✓ |
| Secretos por entorno; no `change-me` en servidor | ✓ (ops); keys no en git |
| Sin Tailscale como camino prod | ✓ runbook/exhaustiva |
| Lab `:5100` / app | — TR-002 (ya F OK) |

### Coherencia (SPEC ↔ HU ↔ TR ↔ docs ↔ AWS)

| Relación | Veredicto |
|----------|-----------|
| TR-002 app vs TR-003 deploy | ✓ separación correcta |
| URLs ops ↔ `urls-deploy.md` / runbook | ✓ `gateway.paqsystems.com` |
| SPEC/HU literal `gateway.paqsuite.com` | ⚠ desalineado; decisión ops documentada (sin zona Route 53) |
| C1 N4 `127.0.0.1` vs prod `0.0.0.0:5100` | ⚠ coherente con CA13; TR/N4 ya actualizados; plantilla `env.example` alineada |
| D1 texto histórico aún menciona `paqsuite` / `127.0.0.1` | ⚠ plan D1 no reescrito; realidad + TR post-F1 mandan |
| HU Gherkin “agente válido WSS” | ⚠ no evidenciado en F1 |

**CRÍTICO:** ninguno.  
**ADVERTENCIA:** CA10 WSS agente; hostname SPEC vs ops; EIP no fija; path local keys no verificado en F1.  
**SUGERENCIA:** alinear prosa SPEC/HU al hostname real en un paso de docs dedicado (o registrar decisión formal); cablear TANGO authenticate + URL interna Forge antes del piloto con token real.

## 4. Pruebas

No re-ejecutadas en F (salvo referencia F1):

| Prueba | Estado |
|--------|--------|
| `dotnet publish` Release | OK (D) |
| `dotnet test` PaqGateway | No aplica a TR-003 (TR-002) |
| Público hub/internal | F1: 400 / 404 |
| Forge → internal status | Ops: 200 |
| WSS SignalR completo | **No corrido** |

## 5. Próximos pasos

1. Humano: marcar **TR-003 Finalizado** si acepta observación CA10; opcionalmente dejar HU-002 en Pendiente de Revisión hasta WSS, o Finalizar HU-002 con la misma salvedad.
2. Commit del informe F1/F + Traza TR (si aún no están en remoto) cuando autoricen.
3. Opcional ops: EIP, WSS con LabAgentMock/PaqAgent, keys path.
4. Siguiente D10: **HU-004 / TR-005** (PaqAgent) tras F1/F de TR-003 aceptados — pedir **C1** o **D1** de TR-005.

## 6. ¿Puede el humano marcar Finalizado?

**Sí, con salvedad** para **TR-003**: deliverable docs+infra OK; CA10 WSS agente pendiente explícito.

**Sí, con la misma salvedad** para **HU-002** completa (app TR-002 Finalizado + deploy TR-003), o **No** para HU-002 si el criterio humano exige CA10 cerrado antes.

Este paso **no** marca `Finalizado` automáticamente.
