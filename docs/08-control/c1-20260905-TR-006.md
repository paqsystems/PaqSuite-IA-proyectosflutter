# Revisión de ambigüedad - TR-006

| Campo | Valor |
|-------|--------|
| Fecha | 2026-09-05 |
| Paso | C1 |
| TR | [TR-006-diagnostics-e2e.md](../04-tareas/001-Conectividad/TR-006-diagnostics-e2e.md) |
| HU | [HU-005](../03-historias-usuario/001-Conectividad/HU-005-diagnostics-run.md) |
| SPEC | SPEC-AGW-001 §7, §11; D12 |
| Repos | **este** (`PaqAgent`) + **TANGO** (`AgentGatewayClient` / diagnostics) |
| Dependencia | HU-004 / TR-005 **Finalizado**; Gateway jobs/send (TR-002) |

## Resultado general

- Estado: **Apto** (defaults Q1–Q8 cerrados abajo)

## Ambigüedades críticas

| ID | Tema | Cierre |
|----|------|--------|
| Q1 | Header API key en TANGO hoy: `X-Internal-Api-Key`; Gateway TR-002 **M1**: `X-Paq-Internal-Api-Key` | **Alinear TANGO a M1** (`X-Paq-Internal-Api-Key`). Sin eso el e2e Laravel→Gateway falla. |
| Q2 | `AgentGatewayClient::sendJob` **no envía `traceId`**; SPEC §7 lo exige | Añadir `traceId` obligatorio (generar ULID/UUID en Laravel si el caller no pasa uno) en el body hacia `/internal/jobs/send`. |
| Q3 | Forma del `data` de `diagnostics.run` en success | JSON camelCase mínimo: `agentId`, `agentVersion`, `sqlConnectionOk` (bool), `readiness` (string SPEC), más campos opcionales (`sqlServerName`, `machineName`, `schemaVersionApplied`). Status job = `success`\|`degraded`\|`offline`\|`timeout`\|… (D12). |

## Ambigüedades menores — cierre

| ID | Decisión |
|----|----------|
| Q4 | **Agente (este repo):** reemplazar stub TR-005 para `operation == diagnostics.run`: ping SQL (open/close), armar `data` Q3, setear readiness; si SQL down con hub OK → `JobStatuses.Degraded`. Otras operations siguen stub/lista blanca. **Sin SQL libre.** |
| Q5 | **Laravel (TANGO):** método `runDiagnostics(agentId, clientId, ?timeout)` (o equivalente) que llama `sendJob(..., 'diagnostics.run', [], timeout)` + `traceId` (Q2) + header M1 (Q1). Config: `AGENT_GATEWAY_URL` = URL **interna** VPC (`http://10.0.1.224:5100` en ref) o lab `http://127.0.0.1:5100`. |
| Q6 | **Orden de prueba:** (1) lab local Gateway+Agente+curl/`sendJob` sin Tailscale; (2) opcional AWS Forge→Gateway privado→agente Windows. E2e AWS no bloquea cierre de código si lab e2e + degraded documentados; Traza marca AWS pendiente ops. |
| Q7 | Timeout default **30 s** (`timeoutSeconds` en job); Gateway ya espera TCS. |
| Q8 | Logs de duración: al menos timestamps/elapsed en Agente (job) y Laravel (sendJob); Gateway ya tiene jobId/traceId. Mapa lab-local §2/§6. |

## Contradicciones TR ↔ HU ↔ SPEC

| Tema | Resolución |
|------|------------|
| Cliente TANGO legacy vs contrato PaqGateway | Q1–Q2 |
| Stub TR-005 vs diagnostics real | Q4 — solo esta operation en TR-006 |
| “Prueba real AWS” en TR | Q6 — lab primero; AWS en Traza/ops |

## Supuestos detectados

| ID | Nota |
|----|------|
| — | `authenticate` Laravel puede seguir pendiente; lab usa stub Dev. |
| — | No hay SP obligatorio para diagnostics ping (open connection basta MVP). |
| — | Sin fallback `host` si tenant modo agente (HU/TR). |

## Preguntas para decisión humana

Ninguna bloqueante. Opcional: ¿exponer Artisan/`route` interna para disparar diagnostics, o solo service method? Default C1: **service method** (+ prueba por Tinker/curl interno); UI fuera.

## Recomendaciones de ajuste de la TR

Aplicar Q1–Q8 en TR-006; `Estado: Especificado`; HU-005 → `Especificado`. Separar checkboxes **este** vs **TANGO**.

## Repo de implementación

- **este** — `diagnostics.run` en PaqAgent + tests/lab
- **TANGO** — header M1, `traceId`, `runDiagnostics` / ajuste `sendJob`
- Ejecución e2e AWS: **humano/ops**

## Veredicto

- **Puede pasar a D1/D: Sí** (HU SIMPLE → D1 recomendado por ser **ambos repos**)
- Siguiente: `Hacé el paso D1` (plan TR-006) → D (este) y D en TANGO según plan
