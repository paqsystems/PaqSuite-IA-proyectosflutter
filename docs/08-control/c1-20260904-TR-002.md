# Revisión de ambigüedad - TR-002

| Campo | Valor |
|-------|--------|
| Fecha | 2026-09-04 |
| Paso | C1 |
| TR | [TR-002-paqgateway-app.md](../04-tareas/001-Conectividad/TR-002-paqgateway-app.md) |
| HU | [HU-002](../03-historias-usuario/001-Conectividad/HU-002-gateway-aws.md) CA 1–8 |
| SPEC | SPEC-AGW-001 §6–§7, D8/D12/D16 |
| Repo de implementación | **este** (`src/PaqGateway`, `src/PaqContracts`, `tests/`) |

## Resultado general

- Estado: **Apto** (defaults M1–M9 **cerrados** abajo; reflejados en la TR)

## Ambigüedades críticas

Ninguna tras el cierre M8/M9 (sin ellos, dos implementadores diferían en auth del hub y en el RPC del job).

## Ambigüedades menores — cierre

| ID | Decisión (fijada en TR-002) |
|----|------------------------------|
| M1 | Header API key interna: `X-Paq-Internal-Api-Key` ← `Gateway:InternalApiKey` |
| M2 | `jobs/send` **síncrono**; responde `JobResult` con estados D12 |
| M3 | Cache token **60 s** (`Gateway:AgentTokenCacheSeconds`) |
| M4 | Authenticate Laravel: path + body `{ agentId, clientId, agentToken }`; 200/401/403; header hacia Laravel = **mismo** `X-Paq-Internal-Api-Key` (`LaravelApi:InternalApiKey`) |
| M5 | Stub authenticate **solo Development**; prohibido en Production |
| M6 | `/status` JSON camelCase con `status` online\|degraded\|offline |
| M7 | Wire JSON camelCase |
| M8 | Credenciales al hub: query en la conexión SignalR `agentId`, `clientId`, `agentToken` (validadas en `OnConnectedAsync` vía M4/M5). Sin token → rechazo de conexión. |
| M9 | RPC job: Gateway → agente método hub **`ExecuteJob`** (`JobRequest`); agente → Gateway **`CompleteJob`** (`JobResult` con mismo `jobId`/`traceId`). Nombres fijos en `PaqContracts`. |

No quedan preguntas humanas abiertas para **codear TR-002**.

## Contradicciones TR ↔ HU ↔ SPEC

Ninguna que bloquee. Deploy AWS fuera de alcance (TR-003). D4 “o catálogo” cede ante SPEC §6.2 (Laravel + cache).

## Supuestos detectados

| ID | Nota |
|----|------|
| A1-S1 | Stub lab documentado (M5) |
| — | Endpoint Laravel real puede no existir aún; no bloquea D con M5 |
| — | Mock de agente en tests basta; PaqAgent real = TR-005 |

## Preguntas para decisión humana

Ninguna obligatoria. TR-003 (EC2/Nginx/ALB) se revisa en su propio C1.

## Recomendaciones de ajuste de la TR

Aplicadas: sección “Decisiones cerradas (post-C1)”; Estado → `Especificado`; M8/M9 en tareas.

## Repo de implementación

- **este** — no PHP en TANGO para TR-002.

## Veredicto

- **Puede pasar a D1/D: Sí** (HU COMPLEJA → **D1 obligatorio** antes de D)
- HU-002 / TR-002 (parte app): `Estado: Especificado`
- Siguiente: `Hacé el paso D1` (plan TR-002) → luego D
