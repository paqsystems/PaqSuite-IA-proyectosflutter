# Verificación del agente - TR-008

| Campo | Valor |
|-------|--------|
| Fecha | 2026-09-05 |
| Paso | F1 |
| TR | [TR-008-corte-duro-modo-agente.md](../04-tareas/001-Conectividad/TR-008-corte-duro-modo-agente.md) |
| HU | HU-007 |
| Repo | **PaqSuite-IA-TANGO** (`FRAMEWORK`) — sin código nuevo en este repo |

## Resultado

- **Aprobado con observaciones**

## Evidencia revisada

| Fuente | Qué muestra |
|--------|-------------|
| Traza TR-008 | AuthController 503; logs sin “fallback a SQL”; tests control |
| PHPUnit (re-ejecutado F1) | **8 passed** (`AuthServiceGatewayModeTest` + `AuthControllerLoginAgentOfflineTest` + `AgentModeNoSqlFallbackControlTest`) |
| Lab Tinker (humano) | `AuthService::login('PQ', …, 'tecser')` con agente offline → `errorCode=5030`, mensaje `AGENT_OFFLINE` |
| Lab Gateway REST (humano) | `jobs/send` auth.login, agente offline → `status=offline`, `errorCode=AGENT_OFFLINE` (`traceId=01OFFLINE`) |
| Lab Gateway REST online | mismo job con agente conectado → `status=success` / `data.status=OK` (control positivo) |
| Grep | `fallback a SQL directo` en `backend/app/Services` → **0 hits** |

## Hallazgos críticos

Ninguno. Hay evidencia real de corte duro modo agente (AuthService + Gateway) y de mapeo HTTP 503 en unitario del controller.

## Advertencias

1. **HTTP e2e** `POST /api/v1/auth/login` con `X-Paq-Cliente: tecser` no cierra el caso A por sí solo: `ResolveTenant` exige SQL diccionario en `host` Tailscale (`100.68.250.93`) antes de AuthController. La señal `AGENT_OFFLINE` se verificó vía **AuthService** + unitario controller (503).
2. Conflicto de puerto: en lab, smoke FRAMEWORK ocupaba `:8000`; hay que usar TANGO (`artisan serve`) limpio.
3. Código TANGO aún **sin commit** (pendiente autorización).

## Tests

| Suite | Resultado |
|-------|-----------|
| TANGO PHPUnit (3 archivos TR-008) | **8 passed** (2026-09-05, F1) |
| Lab Tinker AuthService offline | Ejecutado (humano) → 5030 / AGENT_OFFLINE |
| Lab Gateway `jobs/send` offline/online | Ejecutado (humano) → offline luego success |

## Pendientes

- Paso **F**.
- Commit TANGO cuando lo pidan.
- (Opcional) e2e HTTP login cuando el `host` del tenant modo agente sea alcanzable sin Tailscale.

## Recomendación final

Pasar a **paso F**. No marcar `Finalizado` (solo humano).
