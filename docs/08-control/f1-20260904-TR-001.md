# Verificación del agente - TR-001

Fecha: 2026-09-04  
HU: HU-001 — Alta de cliente agente sin IP  
Repo de implementación: **PaqSuite-IA-TANGO** (rama `FRAMEWORK`)  
Estado TR al verificar: Pendiente de Revisión

## Resultado

- **Aprobado con observaciones**

## Evidencia revisada

1. **Alcance:** Artisan MUST + migración + modelo + validación modo agente. Sin UI nueva (M1 SHOULD: no había ABM). Sin Tailscale, sin IP pública, sin `dev-agent-token`, sin tabla `agents`.
2. **Repo correcto:** código PHP solo en TANGO; este repo (AgenteCliente) no contiene implementación Laravel de TR-001.
3. **Archivos vs Traza:** coinciden:
   - `backend/database/migrations/tenants_catalog/2026_09_04_180000_empresas_conexion_modo_agente.php`
   - `backend/app/Models/EmpresaConexion.php` (modificado)
   - `backend/app/Services/EmpresasConexion/EmpresaConexionAltaAgenteService.php`
   - `backend/app/Console/Commands/EmpresasConexionAltaAgenteCommand.php`
   - tests Unit + Feature listados en la Traza
4. **BD:** `tenants_catalog.empresas_conexion` tiene columnas `agent_token` y `last_seen_at` (confirmado vía Schema).
5. **Comando registrado:** `php artisan list` muestra `empresas-conexion:alta-agente`.
6. **Prohibiciones:** grep en artefactos TR-001 sin `Tailscale` / `dev-agent-token`. Token cifrado en modelo (`Crypt::encryptString`); tests comprueban que el valor persistido ≠ plaintext.

## Hallazgos críticos

- Ninguno. No hay cierre falso: hay código, migración aplicada y tests re-ejecutados en esta verificación.

## Advertencias

1. Cambios TR-001 en TANGO siguen **sin commit** (`M` + `??` en `FRAMEWORK`). La evidencia local es válida; el remoto aún no refleja el trabajo.
2. No se corrió un alta manual aparte de PHPUnit Feature; la Feature usa el comando Artisan real contra `tenants_catalog` con `DatabaseTransactions`.
3. Criterio HU “formulario o comando”: solo comando (aceptado por M1 MUST). UI fuera de alcance MVP.

## Tests

Re-ejecutados en F1 (2026-09-04), directorio `PaqSuite-IA-TANGO/backend`:

```text
php artisan test --filter="EmpresaConexionAltaAgenteServiceTest|EmpresaConexionModoAgenteTest|EmpresasConexionAltaAgenteCommandTest"
```

Resultado: **9 passed (30 assertions)**.

Cobertura vs TR:

| Criterio TR | Evidencia |
|-------------|-----------|
| Alta sin host válida | Feature `test_alta_sin_host_es_valida_y_muestra_token_una_vez` |
| Alta agente sin token inválida | Unit `test_es_modo_agente_valido_exige_agent_id_y_token` |
| Token cifrado | Unit `test_agent_token_se_cifra_al_asignar` |
| Duplicado sin replace | Feature `test_alta_duplicada_sin_force_replace_falla` |
| Sin `dev-agent-token` | Unit `test_generate_agent_token_no_es_dev_agent_token` |

## Pendientes

- Commit/push en TANGO (autorización humana).
- Paso **F** (docs ↔ código).
- No marcar `Finalizado` hasta aceptación humana.
- No avanzar HU-002 hasta F/F1 aprobados según ciclo.

## Recomendación final

La TR-001 **sí está hecha** con evidencia real (migración, comando, tests verdes re-corridos). Seguir con **paso F**. Observaciones no bloquean F; el commit en TANGO puede hacerse cuando lo autoricen.
