# Revisión de ambigüedad - TR-003

| Campo | Valor |
|-------|--------|
| Fecha | 2026-09-05 |
| Paso | C1 |
| TR | [TR-003-deploy-gateway-aws.md](../04-tareas/001-Conectividad/TR-003-deploy-gateway-aws.md) |
| HU | [HU-002](../03-historias-usuario/001-Conectividad/HU-002-gateway-aws.md) CA 9–13 |
| SPEC | SPEC-AGW-001 §6.1 |
| Repo | **este** (docs/ops + publish) + **cuenta AWS** (ejecución humana) |
| Dependencia | TR-002 Finalizado (lab verde) |

## Resultado general

- Estado: **Apto** (defaults N1–N6 cerrados abajo)

## Ambigüedades críticas

Ninguna de producto. El cierre **real** del piloto (WSS desde Internet) depende de VPC/DNS/EC2 (H12): bloquea la **aceptación e2e**, no la escritura del runbook en D.

## Ambigüedades menores — cierre

| ID | Decisión |
|----|----------|
| N1 | Compute: **EC2** ya dedicada (runbook). No inventar otro proveedor en MVP. |
| N2 | TLS edge: **Nginx en la misma EC2** + cert (Let's Encrypt o el que use PaqSystems). ALB solo si ya está decidido en la cuenta; si no, Nginx. Documentar en Traza el elegido. |
| N3 | DNS: `gateway.paqsuite.com` → EIP/IP pública de esa EC2 (o ALB si N2=ALB). |
| N4 | Kestrel: `ASPNETCORE_URLS=http://127.0.0.1:5100`; Nginx hace proxy 443 → 5100 con Upgrade WebSocket. `/internal/*` **no** expuesto en el server block público (solo red privada / no proxy desde 0.0.0.0:443 hacia paths internal, o firewall SG: 5100 solo VPC). |
| N5 | Producción: `Gateway__UseDevAuthStub=false` (o ausente). Sin stub. |
| N6 | Publish: `dotnet publish src/PaqGateway -c Release -o /opt/paqgateway` (o ruta documentada); unit systemd `paqgateway.service`. |

## Contradicciones TR ↔ HU ↔ SPEC

Ninguna. TR-002 = app; TR-003 = deploy. Env names alineados a TR-002.

## Supuestos

| ID | Nota |
|----|------|
| H12 | VPC compartida + DNS/TLS existen o se crean en D; si faltan, Traza deja evidencia y HU no se cierra e2e |
| — | Endpoint Laravel authenticate puede faltar: hub público puede autenticar agentes solo cuando TANGO esté cableado; prueba WSS+auth puede usar stub **prohibido** en prod → en D verificar al menos TLS+negotiate o documentar bloqueo auth |

## Preguntas para decisión humana (ops, no bloquean C1)

1. ¿La EC2 Gateway ya está en la **misma VPC** que Laravel?
2. ¿Nginx o ya hay ALB?
3. ¿DNS `gateway.paqsuite.com` ya apunta?

Si no responden ahora, D documenta placeholders y Traza marca pendiente H12.

## Repo de implementación

- **este** (runbook, unit files de ejemplo, comandos publish)
- Ejecución SSH/AWS: **humano / ops**

## Veredicto

- **Puede pasar a D1/D: Sí** (HU COMPLEJA → **D1** recomendado antes de D; plan = orden publish + systemd + nginx + checklist verificación)
- TR-003 → `Estado: Especificado`
- Siguiente: `Hacé el paso D1` (plan TR-003) → D
