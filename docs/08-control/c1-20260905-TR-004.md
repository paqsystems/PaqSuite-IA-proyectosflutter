# Revisión de ambigüedad - TR-004

| Campo | Valor |
|-------|--------|
| Fecha | 2026-09-05 |
| Paso | C1 |
| TR | [TR-004-auto-instalador.md](../04-tareas/001-Conectividad/TR-004-auto-instalador.md) |
| HU | [HU-003](../03-historias-usuario/001-Conectividad/HU-003-auto-instalador.md) |
| SPEC | SPEC-AGW-001 §5 / §8–§10; D1, D9, D10, D14, D19 |
| Repo | **este** (`src/PaqAgentInstaller` + publish `src/PaqAgent`) |
| Dependencia | HU-004…007 Finalizado; identidad HU-001 |

## Resultado general

- Estado: **Apto** (Q1–Q8 cerrados abajo)

## Ambigüedades críticas — cierre

| ID | Tema | Decisión |
|----|------|----------|
| Q1 | Default Gateway URL | **`https://gateway.paqsystems.com/agent-hub`** (ops real). Alinear SPEC §5 / D8 / HU (dejar de usar `paqsuite.com` como default de fábrica). Editable en UI. |
| Q2 | Contenido del zip | Un asset: instalador (+ entrypoint) + carpeta **`agent/`** con publish **win-x64 self-contained** de PaqAgent. |
| Q3 | Prueba Gateway | HTTPS a la Gateway URL del formulario: validar URI + intento de alcance al hub (negotiate SignalR o request HTTPS documentado en D1). Si falla → abortar sin servicio salvo override D14. **No** exigir agente `online`. Mensaje: DNS / TLS / 443 saliente. |
| Q5 | Entrypoint sin runtime | Instalador publish **self-contained** win-x64 (arranca siempre). Paso 0 corre dentro de ese proceso. |

## Ambigüedades menores — cierre

| ID | Decisión |
|----|----------|
| Q4 | Oferta runtime SHOULD: **descarga oficial Microsoft** (URL fija en código/config + opción abrir navegador / `winget` si disponible). Redist embebido = fuera del MVP de peso (fase siguiente). |
| Q6 | Puerto SQL: agregar **`sql.port`** opcional nullable en `AgentOptions` / JSON; connection string lo usa si tiene valor. Vacío = default driver (1433 / instancia). No concatenar a ciegas en `server` si hay named instance. |
| Q7 | Nombre servicio Windows fijo: **`PaqAgent`**. |
| Q8 | `encrypt` / `trustServerCertificate`: en UI avanzada (paso 1 o 2), defaults **`encrypt=true`**, **`trustServerCertificate=true`**. Lab puede bajar encrypt en el form; no hardcodear `encrypt:false` de lab en prod. |

## D19 — Runtime (refino C1)

| Ítem | Decisión |
|------|----------|
| MUST | Detectar .NET 8 **Desktop** Runtime x64; aviso claro; aviso **posible reinicio del servidor**; SHOULD ofrecer instalar (Q4). |
| Bloqueo vs SC | Con Q2+Q5 (instalador + agente **self-contained**), el servicio **no depende** del Desktop Runtime de máquina. Paso 0: si falta Desktop → MUST aviso + reinicio + SHOULD instalar; botón **“Continuar”** solo tras ACK explícito del aviso de reinicio (no silenciar el aviso). Si en el futuro el agente pasa a FDD, C1/D1 reabrirá bloqueo duro sin ACK. |
| Slice D | Shell pasos 0–4 + MUST aviso/detección primero; SHOULD descarga puede ir en el mismo D o slice inmediato documentado en Traza. |

## Contradicciones TR ↔ HU ↔ SPEC

| Tema | Resolución |
|------|------------|
| `paqsuite.com` vs `paqsystems.com` | Q1 → alinear docs al hostname ops |
| Puerto en SPEC vs `SqlOptions` | Q6 → agregar `sql.port` |
| D19 “no avanzar sin runtime” vs SC | Refino arriba: aviso MUST + Continuar con ACK si paquete SC |

## Supuestos

| ID | Nota |
|----|------|
| S1 | Alta Laravel (HU-001) ya entregó AgentId/ClientId/Token al admin; el instalador no escribe `empresas_conexion`. |
| S2 | Ejecución como Administrador (SPEC); crear servicio exige elevación. |
| S3 | Re-instalación: si existe `appsettings.local.json`, preguntar antes de sobrescribir (default sugerido en D1: confirmar). |

## Preguntas para decisión humana

Ninguna bloqueante (defaults Q1–Q8 aplicados).

## Recomendaciones de ajuste de la TR

Aplicar tabla “Decisiones cerradas (post-C1)”; checkboxes alineados a Q1–Q8; `Estado: Especificado`. Actualizar SPEC §5 / D8 default URL.

## Repo de implementación

- **este** — `PaqAgentInstaller`, ajustes menores `PaqAgent`/`AgentOptions` (port), empaquetado zip
- **TANGO** — no

## Veredicto

- **Puede pasar a D1/D: Sí** (HU COMPLEJA → **D1 obligatorio** antes de D)
- Siguiente: `Hacé el paso D1`
