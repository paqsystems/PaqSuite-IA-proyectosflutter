# Verificación del agente - TR-006

| Campo | Valor |
|-------|--------|
| Fecha | 2026-09-05 |
| Paso | F1 |
| TR | [TR-006-diagnostics-e2e.md](../04-tareas/001-Conectividad/TR-006-diagnostics-e2e.md) |
| HU | HU-005 |
| Repos | este (`PaqAgent`) + TANGO (`AgentGatewayClient`) |

## Resultado

- **Aprobado con observaciones**

## Evidencia revisada

| Fuente | Qué muestra |
|--------|-------------|
| Traza TR-006 (este) | `DiagnosticsRunner`, `JobOperations.DiagnosticsRun`, tests, lab tramo 4 |
| Traza TR-006 (TANGO) | Header M1, `traceId`, `runDiagnostics`, `parameters` → `{}` |
| Lab REST | 2026-09-05 `Invoke-RestMethod` → `degraded` / `SQL_NOT_CONFIGURED` |
| Lab Tinker (humano) | 2026-09-05 `runDiagnostics` → `jobId=61e6e318…`, mismo contrato |
| Commits | este `eac2eeb`+docs; TANGO `8f6ff44d`, `07538109` |

## Hallazgos críticos

Ninguno bloqueante. El e2e TANGO→Gateway→Agente está demostrado en lab.

## Advertencias

1. **AWS e2e** (Forge → `10.0.1.224:5100` → agente Windows) sigue opcional en Traza; no corrido.
2. Lab solo cubrió caso **sin SQL** (`degraded`); no se documentó en esta sesión un `success` con SQL lab real.
3. Re-ejecutar `dotnet test` con PaqAgent en marcha puede fallar por DLL lock; la corrida documentada previa fue **8 passed** (PaqAgent.Tests) y **4 passed** (`AgentGatewayClientTest`).

## Tests

| Suite | Resultado documentado |
|-------|------------------------|
| `tests/PaqAgent.Tests` (DiagnosticsRunner) | 8 passed (2026-09-05, pre-lock) |
| TANGO `AgentGatewayClientTest` | 4 passed (incluye assert `parameters` object) |
| E2e manual REST + Tinker | Ejecutados; no son PHPUnit/`dotnet test` |

## Pendientes

- Paso **F** (docs ↔ código).
- (Ops) AWS opcional.
- (Opcional) evidencia lab con SQL OK → `success`.

## Recomendación final

Pasar a **paso F**. No marcar `Finalizado` (solo humano). No abrir HU siguiente hasta F aprobado.
