# Verificación del agente - TR-005

Fecha: 2026-09-05  
HU: HU-004 — Agente conectado / heartbeat  
Repo: **este** (`src/PaqAgent`)  
Estado TR: Pendiente de Revisión

## Resultado

- **Aprobado con observaciones**

## Evidencia revisada

1. **Alcance:** PaqAgent Worker + SignalR M8 + Heartbeat 30 s + reconnect + Serilog + stub jobs; lab-local tramo 2; prueba manual documentada en TR.
2. **Repo correcto:** este; sin PHP TANGO; sin Tailscale / `dev-agent-token`.
3. **Archivos vs Traza/D1:** `Program`, `AgentOptions`, `HubUrlBuilder`, `AgentGatewayConnector`, appsettings + example, tests, lab-local.
4. **Prueba manual:** procedimiento en TR-005 § Prueba manual; smoke D 2026-09-05 → status `online` para `lab-agent-01`.
5. **Prohibiciones:** rechazo explícito de `dev-agent-token`; logs con `agentToken=***`.

## Hallazgos críticos

- Ninguno. No es cierre falso.

## Advertencias

1. Reconnect backoff: código presente; no hay test automático de caída de red.
2. Token inválido contra Gateway prod/Laravel: ejercitado en AWS (TR-003 caso B) con LabAgentMock; con PaqAgent + stub Dev lab el rechazo de token vacío está en código, prueba manual opcional §5.
3. `ExecuteJob` es stub (TR-006 hará diagnostics SQL).
4. Windows Service real en máquina cliente no instalado en D (lab = `dotnet run`).

## Tests

Re-ejecutados en F1 (2026-09-05):

```text
dotnet test tests/PaqAgent.Tests/PaqAgent.Tests.csproj
```

**5 passed** (HubUrl, options).

Smoke lab (D): Gateway + PaqAgent → `online`.

## Pendientes

- Paso F.
- Humano: Finalizado tras F.
- TR-006 para diagnostics e2e / SQL lista blanca.

## Recomendación final

TR-005 **hecha** con evidencia (tests + smoke + procedimiento manual). Seguir con **paso F**. No marcar `Finalizado`.
