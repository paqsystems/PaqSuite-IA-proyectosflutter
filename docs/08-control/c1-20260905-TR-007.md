# Revisión de ambigüedad - TR-007

| Campo | Valor |
|-------|--------|
| Fecha | 2026-09-05 |
| Paso | C1 |
| TR | [TR-007-auth-login-piloto.md](../04-tareas/001-Conectividad/TR-007-auth-login-piloto.md) |
| HU | [HU-006](../03-historias-usuario/001-Conectividad/HU-006-auth-login.md) |
| SPEC | SPEC-AGW-001 §7 / §8 / §9; D15; D11 |
| Repos | **este** (`PaqAgent` handler) + **TANGO** (`AuthService`) |
| Dependencia | HU-005 / TR-006 **Finalizado** |

## Resultado general

- Estado: **Apto** (defaults Q1–Q9 cerrados abajo)

## Ambigüedades críticas

| ID | Tema | Cierre |
|----|------|--------|
| Q1 | `AuthService` (TANGO) hace **fallback SQL** si Gateway falla / offline / timeout, con `agent_id` presente | **Prohibido** (SPEC §7, D11, HU-006). Con modo agente: sin SQL directo. `offline`/`timeout` → error `AGENT_OFFLINE` (o 503 de proveedor); excepción de Gateway ≠ fallback. |
| Q2 | SPEC §7 muestra `parameters.clave` / `_database`; Laravel hoy solo envía `codigo` y valida password con `Hash::check` sobre `password_hash` del SP | **Contrato MVP:** job `parameters: { "codigo": "<usuario>" }` únicamente. Password **no** viaja al agente. `_database` no en el job (agente usa SQL de `appsettings.local.json`). Alinear ejemplo mental al contrato TANGO update-01 (sin reintroducir fallback). |
| Q3 | Forma de `data` en `success` | JSON camelCase mínimo alineado a AuthService: `status` (`OK`), `user.password_hash`, `es_admin`, `redirectTo`, `empresas[]`. Job status D12: `success`\|`failed`\|`offline`\|`timeout`\|… |

## Ambigüedades menores — cierre

| ID | Decisión |
|----|----------|
| Q4 | **Agente (este):** handler específico `auth.login` → `EXEC PAQ_Auth_Login` parametrizado; armar multi result set → `data` Q3. Lista blanca MVP: `diagnostics.run` + `auth.login`. Resto → `OPERATION_NOT_ALLOWED`. |
| Q5 | **TANGO:** `AuthService` usa `sendJob(..., 'auth.login', ['codigo'=>…])` con `traceId` (ya en `AgentGatewayClient`). Mapear `errorCode`/`errorMessage` del JobResult (no asumir solo `error.code` anidado). Quitar fallbacks Q1. |
| Q6 | **SP:** reutilizar `PAQ_Auth_Login` (+ fix ColRolPK si aplica). Script/migración embebida en este repo o path documentado en Traza; **no** migraciones masivas GEN-18. Lab: SP presente en diccionario del piloto. |
| Q7 | **Prueba:** (1) lab Gateway+Agente+SQL con SP + login Tinker/`POST /api/v1/auth/login` tenant modo agente; (2) operación no listada → `OPERATION_NOT_ALLOWED`; (3) agente parado → `AGENT_OFFLINE` sin SQL. AWS opcional en Traza. |
| Q8 | Timeout default **30 s**. |
| Q9 | Sin Tailscale; sin SQL libre; sin `dev-agent-token`. |

## Contradicciones TR ↔ HU ↔ SPEC

| Tema | Resolución |
|------|------------|
| TANGO legacy “login con fallback” vs corte duro modo agente | Q1 — TR-007 elimina fallback; HU-007 refuerza el corte; este TR ya lo aplica en el camino login |
| Ejemplo SPEC con `clave` vs contrato actual | Q2 |
| TR delgada vs handler multi-RS | Q3–Q4 — detalle en D1 |

## Supuestos detectados

| ID | Nota |
|----|------|
| — | Existe (o se embebe) SP `PAQ_Auth_Login` usable en lab; si el `.sql` no está en este git, D1 lo localiza en legado/TANGO docs y lo versiona aquí o referencia path. |
| — | Usuarios de lab existen en diccionario SQL **y** en Laravel `users` para `Hash::check` + emisión Sanctum (contrato actual). |
| — | Un agente por tenant (D13) ya asumido. |

## Preguntas para decisión humana

Ninguna bloqueante. Opcional: ¿reintroducir envío de `clave` al SP en fase 2? Default C1: **no** en MVP (sigue Hash en Laravel).

## Recomendaciones de ajuste de la TR

Aplicar Q1–Q9 en TR-007; separar checkboxes **este** vs **TANGO**; `Estado: Especificado` en HU-006 y TR-007.

## Repo de implementación

- **este** — handler `auth.login`, whitelist, tests, script SP si falta
- **TANGO** — `AuthService` sin fallback modo agente; mapeo errores JobResult; prueba login
- Ejecución e2e AWS: **humano/ops** (opcional)

## Veredicto

- **Puede pasar a D1/D: Sí** (HU SIMPLE → D1 recomendado por **ambos repos** + SP)
- Siguiente: `Hacé el paso D1` (plan TR-007)
