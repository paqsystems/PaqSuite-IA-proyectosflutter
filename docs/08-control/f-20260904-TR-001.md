# Paso F — Verificar implementación vs documentos — TR-001

Fecha: 2026-09-04  
Equivalente: openspec-05

## 1. Contexto

| Artefacto | Ruta |
|-----------|------|
| SPEC | `docs/02-producto/SPEC-AGW-001-producto.md` (§4, §3 roles, alcance MVP alta) |
| HU | `docs/03-historias-usuario/001-Conectividad/HU-001-alta-cliente-agente.md` |
| TR | `docs/04-tareas/001-Conectividad/TR-001-alta-empresas-conexion.md` |
| C1 | `docs/08-control/c1-20260904-TR-001.md` (Apto) |
| F1 | `docs/08-control/f1-20260904-TR-001.md` (Aprobado con observaciones) |
| Código | `PaqSuite-IA-TANGO` rama `FRAMEWORK`: migración tenants_catalog, `EmpresaConexion`, `EmpresaConexionAltaAgenteService`, `EmpresasConexionAltaAgenteCommand`, tests Unit/Feature |

## 2. Resumen

La implementación en TANGO cubre el alcance de HU-001 / TR-001 y alinea con SPEC §4 (alta modo agente sin `host`, token en catálogo, 1 agente por tenant). No hay desviaciones críticas. El humano confirmó alta manual OK. Puede marcar **Finalizado** tras aceptar pendientes operativos (commit).

## 3. Completitud / Corrección / Coherencia

### Completitud (CA HU + tareas TR)

| Ítem | Veredicto |
|------|-----------|
| CA1: alta pide cliente+nombre; genera agent_id, client_id, agentToken | ✓ Artisan `empresas-conexion:alta-agente` |
| CA2: no exige host/port/password SQL | ✓ null explícitos; sin prompts |
| CA3: token una vez | ✓ plaintext solo en salida del comando |
| CA4: activo=true, agent_id poblado, host null | ✓ servicio + Feature + prueba manual |
| CA5: sin agent_id no es camino agente | ✓ `esModoAgente` / `esModoAgenteValido`; ruteo Laravel a Gateway = TRs posteriores |
| Migración host/port nullable + agent_token + last_seen_* | ✓ |
| M1 MUST Artisan; sin UI inventada | ✓ |
| M2 token cifrado en `empresas_conexion` | ✓ `Crypt::encryptString` |
| M3 `{client_id}-agent-01` | ✓ |
| M4 last_seen no escrito en alta | ✓ |
| M5 activo=true | ✓ |
| D13 rechazo / `--force-replace` | ✓ documentado en Traza |
| Tests TR (sin host OK; sin token inválida) | ✓ |
| Sin Tailscale / sin `dev-agent-token` | ✓ |

### Corrección (comportamiento = SPEC)

| Tema SPEC | Veredicto |
|-----------|-----------|
| §4: catálogo = ruteo, no connection string remoto | ✓ alta no pide SQL remoto |
| §4.1 token en columna (o agents); plaintext una vez | ✓ columna `agent_token`; sin tabla `agents` |
| §4.3 host/port no usados en modo agente | ✓ quedan null |
| §4.1 last_seen runtime (no alta) | ✓ columnas nullable; alta no las setea (M4) |
| Un agente por tenant (D13) | ✓ |
| No fallback / no Tailscale en esta TR | ✓ fuera de alcance y no introducidos |

### Coherencia SPEC ↔ HU ↔ TR ↔ código

| Relación | Veredicto |
|----------|-----------|
| SPEC ↔ HU-001 | ✓ narrativa y Gherkin “alta sin host” |
| HU ↔ TR (M1–M5 post-C1) | ✓ |
| TR Traza ↔ archivos TANGO | ✓ |
| SPEC “modo agente ≈ agent_id presente” vs TR “agent_id + token” | ⚠ código más estricto (exige token); coherente con TR/C1, más seguro |

**ADVERTENCIA (no bloqueante):** el Gherkin HU dice “operador autenticado”; el canal entregado es Artisan CLI (M1), sin sesión web. Aceptado por C1.

**SUGERENCIA:** cuando exista el path Laravel→Gateway, usar `esModoAgenteValido()` (o equivalente) para no tratar filas con `agent_id` y token vacío como modo agente.

**CRÍTICO:** ninguno.

## 4. Pruebas

- PHPUnit (F1, no re-ejecutado en F): 9 passed — Unit cifrado/validación/ids + Feature Artisan.
- Prueba **manual** confirmada por el operador (2026-09-04): alta Artisan OK.

Cobertura adecuada para esta TR. No sustituye E de TRs siguientes.

## 5. Próximos pasos

1. Humano: marcar HU-001 / TR-001 **Finalizado** si acepta el entregable.
2. Commit/push en TANGO `FRAMEWORK` cuando lo autoricen (código aún local respecto a origin al momento de F1).
3. Ciclo D10: siguiente **HU-002 / TR-002** (PaqGateway en este repo), solo tras Finalizado humano según regla del dispatcher.

## 6. ¿Puede el humano marcar Finalizado?

**Sí.**

Condición operativa recomendada (no bloquea el veredicto docs↔código): versionar el cambio en TANGO antes de dar por cerrada la cadena en remoto.
