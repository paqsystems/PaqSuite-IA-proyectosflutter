# Verificación del agente - TR-003

Fecha: 2026-09-05  
HU: HU-002 — Gateway AWS (CA 9–13)  
Repo: **este** (docs/plantillas) + **cuenta AWS** (ops humano)  
Estado TR al verificar: Pendiente de Revisión

## Resultado

- **Aprobado con observaciones**

## Evidencia revisada

1. **Alcance TR-003:** runbook + plantillas deploy + aplicación en EC2 real (*Paq-Gateway-IA*). No incluye código producto PaqGateway (eso es TR-002) ni PHP TANGO.
2. **Repo correcto:** artefactos en `docs/06-operacion/`; ejecución en AWS fuera del repo. Sin Tailscale como camino prod en docs nuevos.
3. **Archivos vs Traza/D1:**
   - `docs/06-operacion/deploy-gateway-aws.md` (ficha §9 + checklist §8 actualizados)
   - `docs/06-operacion/urls-deploy.md` → `https://gateway.paqsystems.com/agent-hub`
   - `docs/06-operacion/deploy/paqgateway.service`, `nginx-gateway.conf`, `env.example`, `README.md`
   - `docs/06-operacion/deploy/instalacion-exhaustiva-paq-gateway-ia.md` (constantes + contingencias)
   - Commit `fa96af5` (docs versionados)
4. **N1–N6 en instancia de referencia:**
   - N1 EC2 `i-026ab0a7c3a957fd2` misma VPC que Forge (`vpc-0588b88f9c6772017`)
   - N2 Nginx + Let's Encrypt en la EC2
   - N3 DNS `gateway.paqsystems.com` (desvío documentado vs SPEC `paqsuite.com`)
   - N4 Kestrel `0.0.0.0:5100` + SG `5100` solo Forge; `/internal` → 404 en Nginx público
   - N5 `UseDevAuthStub=false` en `/etc/paqgateway/env` (visto en sesión ops)
   - N6 binarios en `/opt/paqgateway` + systemd `paqgateway` active
5. **Prohibiciones:** sin secretos API key en git (búsqueda sin GUID de instalación); plantillas sin `change-me-in-production`; sin Tailscale en camino prod del runbook.
6. **Pruebas revalidadas en F1 (agente, 2026-09-05):**
   - `curl https://gateway.paqsystems.com/agent-hub` → **hub:400**, `ssl_verify_result=0` (TLS OK; GET simple a SignalR ≠ handshake WSS completo)
   - `curl https://gateway.paqsystems.com/internal/jobs/send` → **internal:404**
7. **Pruebas ops (sesión humana, 2026-09-05, evidencia en chat):**
   - `systemctl status paqgateway` → `active (running)`
   - `ss` → `0.0.0.0:5100`
   - Local Gateway: `GET /internal/agents/lab-agent-01/status` + API key → **200** `offline`
   - Desde Forge (`10.0.1.147`): mismo `GET` a `http://10.0.1.224:5100/...` → **200** `offline` (**CA 13 / checklist internal VPC**)

## Hallazgos críticos

- Ninguno que invalide el entregable de TR-003 (docs + deploy aplicado). **No es cierre falso** del runbook ni de la EC2 base.

## Advertencias

1. **CA 10 / WSS handshake completo** con agente válido **no** se ejecutó. Solo hay evidencia de TLS + proxy hacia Kestrel (`400` en GET `/agent-hub`). Un cliente SignalR real (LabAgentMock / PaqAgent) contra `https://gateway.paqsystems.com/agent-hub` sigue pendiente; con `UseDevAuthStub=false` el auth Laravel real puede fallar hasta cablear TANGO.
2. **EIP fija** no asociada: IP pública `3.142.236.237` puede cambiar en stop/start (documentado como contingencia).
3. Checklist TR-003 aún tenía casillas WSS/internal sin marcar en el markdown de la TR (desalineado vs evidencia ops); se actualiza Traza tras este F1.
4. Ruta documentada de keys locales `C:\Programacion\KEYS\paq-gateway-ia\keys-solicitados-instalacion.txt`: **no** verificable en este F1 (`Test-Path` → false). Confirmar ubicación ops.
5. Hostname canónico ops = `gateway.paqsystems.com`; SPEC/HU aún pueden citar `paqsuite.com` en prosa histórica (urls-deploy y runbook ya corregidos).

## Tests

| Tipo | Qué | Resultado |
|------|-----|-----------|
| Repo D | `dotnet publish ... -o artifacts/paqgateway` | OK (Traza / sesión D) |
| Producto | `dotnet test` PaqGateway | **No re-ejecutado en F1** (pertenece a TR-002; no es el alcance de deploy) |
| Público F1 | HTTPS hub + `/internal` | **400** / **404** (arriba) |
| VPC ops | Forge → Gateway `:5100` status | **200** (humano) |
| WSS e2e | SignalR negotiate+connect | **No corrido** — declarado explícitamente |

## Pendientes

- Ejecutar handshake WSS real fuera de Tailscale (CA 10) cuando haya auth Laravel o estrategia de prueba acordada.
- Confirmar/rotar ubicación del archivo de keys fuera de git.
- EIP opcional + actualizar registro A si cambia la IP.
- Cablear en Forge/TANGO URL interna `http://10.0.1.224:5100` + misma API key (fuera de esta TR de docs/EC2).
- **Paso F** (docs ↔ realidad).
- Humano: **Finalizado** TR-003 / HU-002 solo tras F y su criterio (WSS puede quedar observación).

## Recomendación final

TR-003 **hecha** para el alcance D (runbook + plantillas + instancia aplicada con evidencias systemd/TLS/DNS/internal VPC). Seguir con **paso F**. Observación principal no bloqueante del deliverable docs/infra: falta WSS e2e con agente. No marcar `Finalizado`. No abrir HU-004/TR-005 hasta F aprobado (o instrucción explícita del humano).
