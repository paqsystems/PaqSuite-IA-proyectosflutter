# Revisión de ambigüedad - TR-005

| Campo | Valor |
|-------|--------|
| Fecha | 2026-09-05 |
| Paso | C1 |
| TR | [TR-005-paqagent-servicio.md](../04-tareas/001-Conectividad/TR-005-paqagent-servicio.md) |
| HU | [HU-004](../03-historias-usuario/001-Conectividad/HU-004-agente-heartbeat.md) |
| SPEC | SPEC-AGW-001 §6.3, §8; D16; decisiones-tecnicas |
| Repo | **este** (`src/PaqAgent`, consume `PaqContracts`) |
| Dependencia | HU-002 / TR-002 **Finalizado**; lab Gateway local o AWS |

## Resultado general

- Estado: **Apto** (defaults P1–P8 cerrados abajo; reflejados en la TR)

## Ambigüedades críticas

| ID | Tema | Bloqueo | Cierre |
|----|------|---------|--------|
| P1 | HU dice **Bearer** + método **`RegisterAgent`**; Gateway TR-002 **M8/M9** usa query `agentId`/`clientId`/`agentToken` en la URL del hub y hub method **`Heartbeat`** (`PaqContracts.HubMethodNames`). No existe `RegisterAgent` en contratos. | Sí (dos implementadores ≠) | **Gana el contrato vivo del Gateway:** conexión SignalR como LabAgentMock / M8; identidad en query; presencia vía `Heartbeat`. “Bearer” de la HU = **no** implementar Authorization header en MVP. Registro = `OnConnectedAsync` auth en Gateway tras connect. |

## Ambigüedades menores — cierre

| ID | Decisión |
|----|----------|
| P2 | Heartbeat **30 s** / TTL online **90 s** → `PaqContracts.AgentDefaults` (ya en Gateway). |
| P3 | Reconexión: backoff **5, 10, 20, 30, 60 s** (HU) con Polly o `WithAutomaticReconnect` + política equivalente; sin Tailscale. |
| P4 | Config lab: **solo** `appsettings.json` (sin secretos) + `appsettings.local.json` (gitignored) con `GatewayUrl`, `AgentId`, `ClientId`, `AgentToken`, SQL local. **Prohibido** `dev-agent-token` hardcodeado. |
| P5 | `last_seen_*`: el agente **no** escribe en Laravel. Actualiza presencia vía heartbeat al Gateway; Laravel consulta `GET /internal/agents/{id}/status` (H4). CA HU “actualiza last_seen” = efecto observable en Gateway status. |
| P6 | Readiness en heartbeat: cadena SPEC `network_ok` → `gateway_authenticated` → `sql_connection_ok` → `schema_ready` → `operational` (campo `AgentHeartbeat.Readiness`). En TR-005 lab, SQL checks pueden quedar en `network_ok`/`gateway_authenticated` si aún no hay SP; documentar en Traza. Jobs `ExecuteJob`/`CompleteJob`: **escuchar** el hub (contrato M9) aunque la ejecución SQL real sea TR-006; MVP mínimo = ack/estructura o no-op seguro sin SQL libre. |
| P7 | Windows Service: Worker Service .NET 8 + `UseWindowsService()`; lab también `dotnet run`. Identidad en heartbeat/logs: `machineName`, nombre SQL si hay config, `AgentVersion` (assembly). |
| P8 | Tests: al menos (a) arranque con config local mock/lab Gateway + status online vía API key, o (b) test de host con hub mock; (c) token inválido → no online + log sin token. Actualizar [lab-local.md](../06-operacion/lab-local.md) tramos 2–3. |

## Contradicciones TR ↔ HU ↔ SPEC

| Tema | Resolución |
|------|------------|
| Bearer / RegisterAgent (HU) vs M8/M9 (Gateway) | P1 — alinear HU en nota post-C1; no inventar RegisterAgent |
| TR lista “Bearer, RegisterAgent” | Reescribir tareas TR a query + Heartbeat |
| Polly explícito vs SignalR reconnect | P3 — Polly **o** equivalente SignalR; backoff HU respetado |

## Supuestos detectados

| ID | Nota |
|----|------|
| — | Lab puede usar Gateway local (`UseDevAuthStub`) **o** prod AWS (auth Laravel); Traza indica cuál. |
| — | Instalador GUI = TR-004 / HU-003 **después**; config manual OK. |
| — | `diagnostics.run` e2e profundo = TR-006; TR-005 = conexión + heartbeat + online. |

## Preguntas para decisión humana

Ninguna bloqueante. Opcional: ¿lab primero contra `127.0.0.1:5100` (recomendado) o directo a `gateway.paqsystems.com`? Default C1: **lab local primero**.

## Recomendaciones de ajuste de la TR

Aplicar en TR-005: sección “Decisiones cerradas (post-C1)” P1–P8; corregir bullets; `Estado: Especificado`. HU-004: nota de alineación P1 + `Especificado`.

## Repo de implementación

- **este** (`src/PaqAgent`). No PHP TANGO en TR-005 (status Laravel vía Gateway ya existente).

## Veredicto

- **Puede pasar a D1/D: Sí**
- HU-004 es **SIMPLE** → D1 **recomendado** (no obligatorio por clasificación); conviene D1 breve por P1/P6.
- Siguiente: `Hacé el paso D1` (plan TR-005) → luego D
