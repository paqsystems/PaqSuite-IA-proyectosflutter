# Revisión de ambigüedad - TR-008

| Campo | Valor |
|-------|--------|
| Fecha | 2026-09-05 |
| Paso | C1 |
| TR | [TR-008-corte-duro-modo-agente.md](../04-tareas/001-Conectividad/TR-008-corte-duro-modo-agente.md) |
| HU | [HU-007](../03-historias-usuario/001-Conectividad/HU-007-corte-duro-modo-agente.md) |
| SPEC | SPEC-AGW-001 §7 / §4.3; D5; D11 |
| Repos | **solo TANGO** (`PaqSuite-IA-TANGO`) |
| Dependencia | HU-006 / TR-007 **Finalizado** (AuthService sin fallback) |

## Resultado general

- Estado: **Apto** (defaults Q1–Q7 cerrados abajo)

## Ambigüedades críticas

| ID | Tema | Cierre |
|----|------|--------|
| Q1 | Alcance: ¿solo login o todos los services Gateway? | **Corte duro en todo camino con `agent_id`**: Auth (piloto CA) + services que ya usan `AgentGatewayClient`/`shouldUseGateway`. AuthService **ya** sin fallback (TR-007). Resto: verificar que offline/timeout/excepción **no** abran SQL por `host`; corregir si queda alguno. |
| Q2 | HU: HTTP **503** (no 401) con `AGENT_OFFLINE` | Hoy `AuthController` mapea `ERROR_AUTH_PROVIDER`→503 pero `ERROR_AGENT_OFFLINE` (5030) cae en `default`→**401**. **Corregir match** → 503 + payload que exponga `AGENT_OFFLINE` (código/mensaje). |
| Q3 | Logs “fallback a SQL directo” en Pedidos/Stock/… | En varios services el log **miente**: ya hacen `HttpException(503)` sin SQL. Renombrar logs a “Gateway degradado / AGENT_OFFLINE” (sin secretos; incluir `agent_id`). |

## Ambigüedades menores — cierre

| ID | Decisión |
|----|----------|
| Q4 | Tenant **sin** `agent_id`: SQL directo **solo** donde exista camino legacy (p. ej. AuthService). Services gateway-only que hoy responden 503 sin `agent_id` **no** se reabren a SQL en esta TR (fuera de alcance; no inventar dual-path). |
| Q5 | Tests MUST: (1) AuthController/login o AuthService→HTTP: offline → **503** + AGENT_OFFLINE; (2) AuthServiceGatewayModeTest ya cubre no-fallback; (3) grep/test de control: no reintroducir `validatePasswordViaSqlDirecto` / “fallback a SQL” **tras** fallo Gateway con `agent_id`. |
| Q6 | No usar `host` de `empresas_conexion` para ruteo ni reintento si hay `agent_id` (auditoría + no nuevas lecturas). |
| Q7 | Prohibido Tailscale / `dev-agent-token`. Repo: **solo TANGO**. Este repo: solo docs HU/TR/C1. |

## Contradicciones TR ↔ HU ↔ SPEC

| Tema | Resolución |
|------|------------|
| TR-007 cerró AuthService; TR-008 “selector” | Q1–Q2: TR-008 = HTTP + auditoría resto + logs |
| CA5 SQL legacy vs services solo-Gateway | Q4 |

## Supuestos detectados

| ID | Nota |
|----|------|
| — | La mayoría de services Gateway ya lanzan 503 en degradación; el hueco real post-TR-007 es **mapeo HTTP del login** + **limpieza de logs engañosos** + **test de regresión**. |
| — | No se exige en MVP un helper compartido único; si D1 lo propone, OK, no bloquea C1. |

## Preguntas para decisión humana

Ninguna bloqueante.

## Recomendaciones de ajuste de la TR

Aplicar Q1–Q7; separar checkboxes: Auth HTTP 503, auditoría services, logs, tests. `Estado: Especificado` en HU-007 y TR-008.

## Repo de implementación

- **TANGO** — `AuthController` (+ AuthService si falta), services Gateway, tests
- **este** — documentación del ciclo únicamente

## Veredicto

- **Puede pasar a D1/D: Sí** (HU SIMPLE; D1 recomendado por muchos archivos / grep)
- Siguiente: `Hacé el paso D1` (plan TR-008) → D en TANGO
