# Verificación del agente - TR-007

| Campo | Valor |
|-------|--------|
| Fecha | 2026-09-05 |
| Paso | F1 |
| TR | [TR-007-auth-login-piloto.md](../04-tareas/001-Conectividad/TR-007-auth-login-piloto.md) |
| HU | HU-006 |
| Repos | este (`PaqAgent`) + TANGO (`AuthService`) |

## Resultado

- **Aprobado con observaciones**

## Evidencia revisada

| Fuente | Qué muestra |
|--------|-------------|
| Traza TR-007 (este) | `AuthLoginRunner`, SP en `Sql/dictionary`, whitelist, `encrypt` |
| Traza TR-007 (TANGO) | Sin fallback SQL con `agent_id`; `errorCode`; `ERROR_AGENT_OFFLINE` |
| Lab REST sin SQL | whitelist / `SQL_NOT_CONFIGURED` / `INVALID_PARAMETERS` |
| Lab REST con SQL (humano) | `01AUTHOK` success `codigo=PQ`; `01DIAGOK` `sqlConnectionOk=true` |
| Commits | este `0b9b0e6`; TANGO `8b1c7862` |

## Hallazgos críticos

Ninguno. Hay evidencia real de `auth.login` vía Gateway→Agente→SP y de corte duro en unitarios Laravel.

## Advertencias

1. **E2e TANGO→Gateway login** (HTTP `/api/v1/auth/login` con tenant modo agente) no corrido en esta sesión; solo PHPUnit + lab REST del agente.
2. AWS e2e opcional, no corrido.
3. Tenants **sin** `agent_id` siguen en SQL directo (SPEC convivencia); fuera del corte duro.

## Tests

| Suite | Resultado documentado |
|-------|------------------------|
| `tests/PaqAgent.Tests` | **12 passed** (incl. `AuthLoginRunnerTests`) |
| TANGO `AuthServiceGatewayModeTest` | **4 passed** (offline / exception / failed / success) |
| Lab manual REST | Ejecutado (humano); ver TR-007 tablas evidencia |

## Pendientes

- Paso **F**.
- (Opcional) e2e login Laravel→Gateway; AWS.

## Recomendación final

Pasar a **paso F**. No marcar `Finalizado` (solo humano). No abrir HU-007 hasta F aprobado (o acuerdo explícito).
