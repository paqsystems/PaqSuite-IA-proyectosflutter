# Verificación docs ↔ código - TR-006

| Campo | Valor |
|-------|--------|
| Fecha | 2026-09-05 |
| Paso | F |
| TR | [TR-006](../04-tareas/001-Conectividad/TR-006-diagnostics-e2e.md) |
| HU | [HU-005](../03-historias-usuario/001-Conectividad/HU-005-diagnostics-run.md) |
| SPEC | SPEC-AGW-001 §7 / §11 |
| F1 | [f1-20260905-TR-006.md](./f1-20260905-TR-006.md) — Aprobado con observaciones |

## Contexto

- **este:** `PaqAgent/Diagnostics/*`, `AgentGatewayConnector`, `JobOperations.DiagnosticsRun`
- **TANGO:** `AgentGatewayClient.php` (M1, `traceId`, `runDiagnostics`, `parameters` `{}`)

## Resumen

Implementación alineada a TR/HU/C1 Q1–Q8 para el alcance lab. Evidencia e2e REST + Tinker (caso sin SQL). AWS y escenario `success` con SQL quedan fuera de la evidencia de esta sesión.

## Completitud

| Ítem | Veredicto |
|------|-----------|
| CA1 Laravel envía `diagnostics.run` + `traceId` | ✓ `runDiagnostics` / `sendJob` |
| CA2 payload success (campos Q3) | ✓ código + tests; lab no mostró `success` (sin SQL) |
| CA3 offline si agente caído | ⚠ Gateway ya lo hace (TR-002); no re-probado en esta sesión TANGO |
| CA4 timeout 30 s | ✓ `timeoutSeconds` / config default |
| CA5 degraded si SQL falla / ausente | ✓ lab + Tinker `SQL_NOT_CONFIGURED` |
| Tareas TR ambos repos | ✓ checkboxes |
| AWS opcional | ⚠ pendiente ops |

## Corrección

| Tema | Veredicto |
|------|-----------|
| Header Laravel→GW M1 | ✓ `X-Paq-Internal-Api-Key` |
| `parameters` JSON object | ✓ fix `07538109` (evita `[]`) |
| Sin Tailscale / sin fallback SQL en este camino | ✓ |
| Lista blanca operations | ✓ solo `diagnostics.run` en agente MVP |

## Coherencia

| Par | Veredicto |
|-----|-----------|
| SPEC ↔ HU-005 ↔ TR-006 | ✓ |
| TR checkboxes ↔ código | ✓ |
| Inbound Laravel `X-Internal-Api-Key` vs outbound M1 | ⚠ documentado; fuera de TR-006 |

## Pruebas

- Unitarios documentados en F1 (no re-ejecutados aquí por DLL lock con PaqAgent vivo).
- Manual lab REST + Tinker humano documentados en TR-006.

## Próximos pasos

1. Humano: marcar TR-006 / HU-005 **Finalizado** si acepta observaciones (AWS/`success` SQL opcionales).
2. Siguiente HU en D10: **HU-006** (tras Finalizado o acuerdo explícito).

## Puede el humano marcar Finalizado

**Sí**, con salvedad ops AWS y sin evidencia lab de `success`+SQL en esta corrida (el caso `degraded` sí está cerrado).
